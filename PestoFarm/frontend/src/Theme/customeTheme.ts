import { createTheme } from '@mui/material/styles';

export const customeTheme = createTheme({
    palette: {
        mode: 'light',
        primary:{
            main: "#E74292"
        },
        secondary: {
            main: "#EAF0F1"
        }
    }
});
